## 1. Data model

- [x] 1.1 Add `PenumbraReactionMode` enum (`None`, `Single`, `Staged`) to `ReactToMe/Triggers/ReactionTrigger.cs` and a `PenumbraReactionMode` field defaulting to `None`; verify the project still builds and existing trigger JSON (no such field present) still deserializes without error
- [x] 1.2 Add a helper (e.g. `GetEffectivePenumbraMode()` on `ReactionTrigger`, or a static helper in `TriggerLabeler`/`ConfigWindow`) implementing the back-compat resolution from design.md: if the stored mode is `None` and `PenumbraStages.Count > 0`, resolve to `Staged`; otherwise use the stored value as-is; verify with a unit-style manual check (construct a trigger with stages set and `PenumbraReactionMode` left at its default, confirm the helper returns `Staged`)

## 2. Config UI

- [x] 2.1 In `ConfigWindow.cs`, at the top of the Penumbra reaction area, call the effective-mode helper and, if it resolved `Staged` from an ambiguous `None`+stages state, write that back to `trigger.PenumbraReactionMode` and `configuration.Save()` once, so the ambiguity self-heals on first draw; verify by loading a pre-existing trigger with stages configured and confirming its mode persists as `Staged` after the config window is opened once
- [x] 2.2 Add a mode selector (radio buttons or combo: "No mod reaction" / "Single mod" / "Staged escalation") bound to `trigger.PenumbraReactionMode`; verify all three states are selectable and persist via `configuration.Save()`
- [x] 2.3 When mode is `None`, show only the mode selector — no mod/option/threshold fields, and `PenumbraStages` stays empty; verify by selecting None on a trigger that previously had stages and confirming the list is cleared and no fields are shown
- [x] 2.4 When mode is `Single`, show flat mod, option-group, and option pickers (reusing the existing picker code from the Staged tab body) with no threshold field and no tab bar, backed by `PenumbraStages[0]` with `Threshold` forced to `1`; auto-create that single entry when switching into Single mode with no existing entry; verify the flat fields work identically to their Staged-mode counterparts and no threshold input or tab bar appears
- [x] 2.5 When mode is `Staged`, keep the existing tabbed editor from `tabbed-penumbra-stages` unchanged (threshold field visible per stage, multiple tabs, add/remove-by-closing-tab); verify no regression against that change's already-verified behavior
- [x] 2.6 Implement mode-switch data carryover exactly as specified in `specs/trigger-config-ui/spec.md`'s "Switching Penumbra reaction mode preserves compatible configuration" requirement: Single->Staged keeps the entry and unlocks its threshold; Staged->Single keeps only the first stage (trimming the rest) and re-locks its threshold to `1`; switching to None clears `PenumbraStages`; verify each transition manually against its scenario
- [x] 2.7 Build and confirm 0 warnings/0 errors with `dotnet build ReactToMe/ReactToMe.csproj -c Debug -p:Platform=x64`
- [x] 2.8 Found during manual testing: `DrawPenumbraModFields` unconditionally showed an "Option group" dropdown even for mods with zero option groups (a plain enable/disable mod), showing a confusing "(None)" instead of hiding it. Fixed: zero groups shows an explanatory note and no dropdowns; exactly one group skips the redundant "Option group" selector and shows that group's "Option" dropdown directly; two or more groups keeps both selectors as before. Verified by build; confirm in-game against a mod with no options, a mod with exactly one option group, and a mod with multiple option groups.

## 3. End-to-end verification

- [ ] 3.1 Manually verify the full scenario set in `specs/trigger-config-ui/spec.md`: None/Single/Staged each show only their relevant fields, mode-switch carryover behaves as specified for all three transition pairs, and a trigger with pre-existing stages (simulate by using a trigger saved before this change, or a config with `PenumbraStages` set and no `PenumbraReactionMode`) loads and behaves as Staged mode unchanged
