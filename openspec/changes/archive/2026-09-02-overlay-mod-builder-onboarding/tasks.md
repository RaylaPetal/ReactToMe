## 1. Discoverable guidance in the Overlay Mod Builder tab

- [x] 1.1 In `DrawOverlayProjectDetail`, add a `DrawHelpMarker` call with new tooltip text for `Display name` (explains it's shown in the project list on the left) and for the `Target` picker (explains it's the base texture stages build on top of, sourced from live-resolving and mod-declared `chara/`-shaped paths). Verify in-game both now show a `(?)` with guidance.
- [x] 1.2 In `DrawOverlayProjectDetail`, add a `DrawHelpMarker` call with new tooltip text for a non-baseline stage's `Name` field (explains it's just a label — the option name shown once the mod is applied). Verify in-game the field shows a `(?)`.
- [x] 1.3 In `DrawOverlayModBuilderTab`, add a `DrawHelpMarker` call with new tooltip text for `Add Project` (creates a blank project to configure). In `DrawOverlayProjectDetail`, add one for `Add Stage` (explains a new stage bakes its overlay onto the previous stage's own baked output, not the pristine snapshot directly). Verify in-game both show a `(?)`.
- [x] 1.4 In `DrawOverlayProjectDetail`, convert the existing bare `IsItemHovered`/`SetTooltip` calls to `DrawHelpMarker` for: the "Stage 0 — Baseline" label, `Capture Snapshot`/`Recapture Snapshot`, `Apply`, `Rebake All`, `Recreate Mod`, `Create Trigger`, `Priority`, and `Penumbra folder` — preserving their existing tooltip text. Verify in-game each shows a `(?)` whose hover text matches what it showed before this change.
- [x] 1.5 Leave `Base texture (read from)` unchanged — it already uses the same `ImGui.TextDisabled` + tooltip idiom `DrawHelpMarker` is built on, so it already satisfies the requirement without a code change. Confirm this by re-reading `DrawOverlayProjectDetail` before starting 1.1-1.4, so it isn't accidentally duplicated with a marker.

## 2. Tutorial walkthrough

- [x] 2.1 Generalize `DrawTutorialPopup()` from its current trigger-specific, no-argument form to `DrawTutorialPopup(string popupId, TutorialStep[] steps, ref int stepIndex)`, and update the Triggers tab's existing call site to pass `"Trigger Tutorial"`, the existing `TutorialSteps` array, and `ref tutorialStepIndex`. Verify by building and confirming the Triggers tab's existing tutorial still opens, steps through, and closes exactly as before (regression check on already-shipped behavior).
- [x] 2.2 Add `overlayTutorialPopupShouldOpen` (bool) and `overlayTutorialStepIndex` (int) fields to `ConfigWindow`, and a `Tutorial` button in `DrawOverlayModBuilderTab`, right-aligned opposite `Add Project` (same `ImGui.GetContentRegionMax().X - buttonWidth` idiom used for the Triggers tab's button), that sets `overlayTutorialStepIndex = 0` and `overlayTutorialPopupShouldOpen = true`. Verify by building and confirming the button renders in-game on the Overlay Mod Builder tab.
- [x] 2.3 In `DrawOverlayModBuilderTab`, add the per-frame open check (`if (overlayTutorialPopupShouldOpen) { ImGui.OpenPopup("Overlay Mod Builder Tutorial"); overlayTutorialPopupShouldOpen = false; }`) and a call to the shared `DrawTutorialPopup("Overlay Mod Builder Tutorial", OverlayTutorialSteps, ref overlayTutorialStepIndex)`. Verify in-game that clicking `Tutorial` opens a modal titled "Overlay Mod Builder Tutorial".
- [x] 2.4 Add a static `OverlayTutorialSteps` array (4 entries — Target texture, Snapshot, Stages, Apply) using the existing `TutorialStep` type, each with a title and explanatory body using illustrative example values (not `configuration.OverlayModBuilderProjects` or any live project/Penumbra data). Verify in-game that opening the tutorial shows the Target texture step first, and that Back/Next/Done navigation (already implemented generically in 2.1) steps through all 4 in order.
- [x] 2.5 Confirm the overlay tutorial's step content never references `configuration.OverlayModBuilderProjects`, `selectedOverlayProjectId`, `plugin.OverlayModBuilderService`, or calls `configuration.Save()` (code review / grep for those identifiers within the new step array), and verify in-game that the project list and every project's fields are unchanged before and after opening, stepping through, and closing the tutorial.
- [x] 2.6 Verify in-game that reopening the overlay tutorial after previously reaching its last step and closing it starts over from the Target texture step, not the last-viewed step.

## 3. Final verification

- [x] 3.1 Run `dotnet build ReactToMe/ReactToMe.csproj -c Debug -p:Platform=x64` and confirm it completes with 0 errors and 0 warnings.
- [x] 3.2 Load the built plugin in-game and manually verify: every field touched in tasks 1.1-1.4 shows a `(?)` with correct guidance on hover; the Overlay Mod Builder `Tutorial` button opens, steps through, and closes correctly; the Triggers tab's own tutorial (task 2.1's regression target) still works unchanged; existing overlay-mod-builder workflows (Add Project, capturing a snapshot, adding/removing a stage, Apply, Rebake All, Recreate Mod, Create Trigger) are unaffected.
